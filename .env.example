# PostgreSQL database dedicated to Sub-ASMS. Do not reuse a production database without a backup.
DATABASE_URL="postgresql://subasms:change-me@localhost:5432/sub_asms?schema=public"
# Optional endpoint to synchronize a confirmed ticket back to the internal portal.
PORTAL_WEBHOOK_URL=""
